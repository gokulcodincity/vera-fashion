import { useEffect,useState } from 'react'; import Button from '../../components/Button'; import { getStoreSettings,saveStoreSettings } from '../../services/settings'; import { userMessage } from '../../services/errors';
export default function AdminSettings(){const[form,setForm]=useState(null),[error,setError]=useState(''),[saving,setSaving]=useState(false);useEffect(()=>getStoreSettings().then(setForm).catch(e=>setError(userMessage(e))),[]);if(!form)return <p className="text-slate">Loading store settings…</p>;const save=async e=>{e.preventDefault();setSaving(true);try{setForm(await saveStoreSettings(form))}catch(err){setError(userMessage(err))}finally{setSaving(false)}};return <div><p className="eyebrow">Configuration</p><h1 className="mt-2 text-4xl">Store Settings</h1>{error&&<p className="mt-4 text-sm text-clay">{error}</p>}<form onSubmit={save} className="mt-7 max-w-3xl border border-line bg-cream p-6"><div className="grid gap-5 sm:grid-cols-2">{[['store_name','Store name'],['tagline','Tagline'],['phone','Phone'],['whatsapp','WhatsApp number'],['email','Email'],['instagram','Instagram URL'],['facebook','Facebook URL'],['currency','Currency']].map(([key,label])=><label key={key} className="text-xs text-slate">{label}<input value={form[key]||''} onChange={e=>setForm({...form,[key]:e.target.value})} className="field mt-2"/></label>)}<label className="sm:col-span-2 text-xs text-slate">Address<textarea value={form.address||''} onChange={e=>setForm({...form,address:e.target.value})} className="field mt-2 resize-none" rows="3"/></label></div><Button className="mt-6" disabled={saving}>{saving?'Saving…':'Save settings'}</Button></form></div>}
